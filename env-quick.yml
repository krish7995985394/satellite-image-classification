name: sidl
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - numpy
      - pandas
      - scikit-learn
      - matplotlib
      - pillow
      - tqdm
      - opencv-python
      - albumentations
      - hydra-core
      - yacs
      - rasterio
      - torch
      - torchvision
